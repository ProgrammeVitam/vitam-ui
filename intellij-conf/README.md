# IntelliJ configuration

## Run/Debug Configurations

Copy the `.run` directory to the project's root to automatically create all the required "Run/Debug Configurations".

You may need to restart IntelliJ to take it into account.
