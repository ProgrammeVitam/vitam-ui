{
{# TODO : check probe is correct / verify file installation is made in vitamui-logstash role #}
  "service": {
    "name": "vitamui-logstash",
    "port": {{ logstash.port | int }},
    "address": "{{ ip_admin }}",
    "enable_tag_override": false,
    "checks": [
      {
        "notes": "Syslog tcp port opened",
        "tcp": "{{ ip_admin }}:{{ logstash.port }}",
        "interval": "1s"
      }
    ]
  }
}
