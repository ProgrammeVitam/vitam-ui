{
  "service": {
    "name": "{{ logstash.service_name }}",
    "port": {{ logstash.port }},
    "service_name": "{{ logstash.service_name }}",
    "address": "{{ ip_admin }}",
    "enable_tag_override": false,
    "tags": ["logstash"],
    "checks": [
      {
        "notes": "Syslog tcp port opened",
        "tcp": "{{ ip_admin }}:{{ logstash.port }}",
        "interval": "{{ logstash.check_consul | default('10') }}s"
      }
    ]
  }
}
