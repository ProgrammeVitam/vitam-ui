---

logstash_log_dir: "{{ vitamui_defaults.folder.root_path | default('/vitam') }}/log/logstash"
logstash_tmp_dir: "{{ vitamui_defaults.folder.root_path | default('/vitam') }}/tmp/logstash"
logstash_conf_dir: "{{ vitamui_defaults.folder.root_path | default('/vitam') }}/conf/logstash"
logstash_confextra_dir: "{{ logstash_conf_dir }}/extra"
logstash_data_dir: "{{ vitamui_defaults.folder.root_path | default('/vitam') }}/data/logstash"

service_at_boot: "{{ vitam_defaults.services.at_boot | default(false) }}"
