---

- name: reload consul configuration for mongo
  service:
    name: "{{ consul.service_name | default('vitam-consul') }}"
    state: reloaded

- name: restart mongod
  service:
    name: "{{ service_name }}"
    state: restarted
