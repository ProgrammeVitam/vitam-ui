---

- name: restart consul
  service:
    name: "{{ consul.service_name | default('vitam-consul') }}"
    state: restarted

- name: reload consul
  service:
    name: "{{ consul.service_name | default('vitam-consul') }}"
    state: reloaded
