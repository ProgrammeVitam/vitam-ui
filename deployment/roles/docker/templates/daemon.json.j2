{
  "bip": "{{ docker_opts.docker_bip }}",
  {% if container_repository.registry_url | default('') != '' %}
  "insecure-registries" : [ "{{ container_repository.registry_url }}" ],
  {% endif %}
  "default-address-pools": [
      { "base": "{{ docker_opts.docker_address_pools_cidr }}", "size": {{ docker_opts.docker_address_pools_size }} }
  ]
}
