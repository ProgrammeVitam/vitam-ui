
- name: "Set service {{ vitamui_struct.service_name | default(service_name) }} to state {{ etat }}"
  service:
    name: "{{ vitamui_struct.service_name | default(service_name) }}"
    state: "{{ etat }}"
