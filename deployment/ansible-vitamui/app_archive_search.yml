# Archive Search external calls Vitam
- hosts: hosts_vitamui_archive_search_server
  gather_facts: no
  any_errors_fatal: true
  roles:
    - vitamui
  vars:
    vitamui_struct: "{{ vitamui.archive_search_server }}"
    vitamui_certificate_type: external
    password_keystore: "{{ keystores_server_archive_search_server }}"
    vitam_cert: "{{ vitam_certs.vitamui }}"
    password_truststore: "{{ truststores_client_external }}"
    consul_tags: "archive-search-server, api, external"
