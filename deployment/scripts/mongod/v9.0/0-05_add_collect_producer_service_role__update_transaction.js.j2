
const scriptName = 'v9.0.0-05_add_collect_producer_service_role__update_transaction.js';
print(['START', scriptName].join(' '));

dbIam.profiles.updateMany({
    applicationName: "COLLECT_APP",
     name: {
                $in: [
                    "Service producteur",
                ]
            }
}, {
    $addToSet: {
        roles: {
            $each: [
                { name: "ROLE_UPDATE_TRANSACTIONS" }
            ]
        }
    }
});

print(['END', scriptName].join(' '));
