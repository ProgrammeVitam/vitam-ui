db = db.getSiblingDB('security')

print("44_new_update_archive_search_context_to_add_rule_access_contracts.js");

db.contexts.updateOne({
   "_id":"ui_archive_search_context"
},
{
   $addToSet:{
      "roleNames":{
         $each:[
            "ROLE_GET_ACCESS_CONTRACTS"
        	]
        }
    }
});

print("44_new_update_archive_search_context_to_add_rule_access_contracts.js");
