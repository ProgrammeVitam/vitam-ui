db = db.getSiblingDB('security')

print("START 314_security_ref.js");

// ========================================= CONTEXTS =========================================
db.contexts.updateOne( {
	"_id": "ui_referential_context"
}, {
    $addToSet: {
        "roleNames":  {
            $each: [
				"ROLE_GET_UNITS"
        	]
        }
    }
});

db.contexts.updateOne( {
	"_id": "ui_identity_context"
}, {
    $addToSet: {
        "roleNames":  {
            $each: [
				"ROLE_GET_EXTERNAL_PARAMS"
        	]
        }
    }
});

db.contexts.updateOne( {
	"_id": "ui_admin_identity_context"
}, {
    $addToSet: {
        "roleNames":  {
            $each: [
				"ROLE_GET_EXTERNAL_PARAMS"
        	]
        }
    }
});

db.contexts.updateOne( {
	"_id": "ui_referential_context"
}, {
    $addToSet: {
        "roleNames":  {
            $each: [
				"ROLE_GET_EXTERNAL_PARAMS"
        	]
        }
    }
});

print("END 314_security_ref.js.j2");
