db = db.getSiblingDB('security')

print("Start - 02_add_roles_for_archive_search_management_app.js");

// -------- ADD MANAGEMENT CONTRACT APPLICATION ROLES TO UI REFERENTIAL CONTEXT -----

db.contexts.updateOne({
	"_id": "ui_archive_search_context"
}, {
    $addToSet: {
        "roleNames":  {
            $each: [
            	"ROLE_GET_SCHEMAS"
        	]
        }
    }
});

print("End - 02_add_roles_for_archive_search_management_app.js");
