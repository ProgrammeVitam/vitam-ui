db = db.getSiblingDB('security')

print("Start - 04_add_roles_for_collect_app");

db.contexts.updateOne({
	"_id": "ui_collect_context"
}, {
    $addToSet: {
        "roleNames":  {
            $each: [
            	"ROLE_GET_SCHEMAS"
        	]
        }
    }
});

print("End - 04_add_roles_for_collect_app");
