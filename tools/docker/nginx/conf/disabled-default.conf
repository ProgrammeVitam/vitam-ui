## Ce fichier est monte par-dessus /etc/nginx/conf.d/default.conf pour neutraliser
## le vhost livre avec l'image nginx, qui ecoute sur le port 80 et masquerait la
## redirection HTTP -> HTTPS de vitamui.conf. Il doit rester vide.
