## TLS termination, using the dev-deployment `reverse` certificate — the only
## one with CN=dev.vitamui.com (SAN: dev.vitamui.com, localhost).
ssl_certificate     /etc/nginx/certs/servers/reverse/reverse.crt;
ssl_certificate_key /etc/nginx/certs/servers/reverse/reverse.key;

## Aligned with deployment/roles/nginx_webapp/templates/ssl-ui.conf.j2.
ssl_protocols             TLSv1.2 TLSv1.3;
ssl_ciphers               ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-SHA384;
ssl_prefer_server_ciphers on;
ssl_session_cache         shared:SSL:10m;
ssl_ecdh_curve            secp384r1;
