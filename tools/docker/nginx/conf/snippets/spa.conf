## Common headers for the Angular apps. Unlike production, caching is fully
## disabled in dev: stale assets after a rebuild are the top source of fake bugs.
add_header X-Content-Type-Options "nosniff" always;
add_header Cache-Control "no-store" always;
