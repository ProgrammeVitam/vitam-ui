#logging.level.org.springframework: DEBUG

spring:
  cloud:
    bus:
      enabled: false
    discovery:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false
  api:
    name: API Ingest
    description: This API is used to manage Ingest operations
    version: v1

login.url: http://cas/login

security:
  host: xxx
  port: 8088

tmp_folder_path: "src/main/resources"

logbook.scheduling.sendEventToVitamTasks.enabled: false
